# 기여 가이드 (Contributing)

`hostap-pqc`에 관심 가져주셔서 감사합니다. 이 문서는 기여 범위, 절차, 커밋 규칙을 안내합니다.
기여하기 전에 **[기여 범위](#기여-범위)** 를 먼저 확인해 주세요. 변경의 성격에 따라
제출해야 할 위치가 달라집니다.


## 목차

- [기여 범위](#기여-범위)
- [기여 유형](#기여-유형)
- [개발 환경 및 빌드](#개발-환경-및-빌드)
- [기여 절차](#기여-절차)
- [커밋 규칙](#커밋-규칙)
- [코드 스타일](#코드-스타일)
- [Pull Request 체크리스트](#pull-request-체크리스트)
- [리뷰 및 병합](#리뷰-및-병합)
- [라이선스 및 DCO](#라이선스-및-dco)

## 기여 범위

`hostap-pqc`는 [w1.fi hostap](https://w1.fi/hostap.git)의 포크입니다.
변경의 성격에 따라 제출 위치가 다르므로, 아래 표를 먼저 확인해 주세요.

| 변경 성격 | 제출 위치 |
|-----------|-----------|
| PQC 프로토콜 구현, liboqs 연동, 802.11 관련 최적화 | `hostap-pqc` |
| Upstream 최신 버전 포팅 및 호환성 검증 | `hostap-pqc` |
| PQC와 무관한 `hostap` 프로젝트의 버그·기능 개선 | 원본 프로젝트 [w1.fi](https://w1.fi/hostap.git) |
| 프로토콜 설계·명세 관련 논의 | 프로토콜 명세서 레포 [protocol](https://github.com/80211-pqc/protocol) |

> ℹ️ `hostap` 프로젝트 자체의 개선을 이 레포에 제출하면, 원본과의 병합이 어려워지고
> 유지보수 부담이 커집니다. 순수 `hostap` 개선은 반드시 원본으로 보내주세요.

> ℹ️ **Upstream 최신화 기여란?**
> `hostap-pqc`는 특정 Base 스냅샷(`ca266cc2…`)에 고정되어 있습니다.
> 원본 `hostap`가 갱신되면서 시간이 지나면 버전 간극이 벌어집니다.
> 최신 `hostap`를 현재 PQC 구현 위에 포팅하고, **동작·호환성 테스트를 거쳐**
> PR를 제출하면 기여로 인정됩니다. 테스트 없이 버전만 올린 PR는 병합되지 않습니다.

## 기여 유형

이 레포에서 환영하는 기여는 다음과 같습니다.

- **Upstream 포팅** — `hostap`의 새 릴리스를 현재 Base 스냅샷 위에 충돌 없이 포팅하고, 동작·호환성 테스트로 검증
- **PQC 연산 최적화** — `liboqs-80211` 연동 및 성능 개선
- **버그 수정** — PQC 프로토콜 구현 관련 버그
- **문서 개선** — README, 본 가이드, 코드 주석 등
- **보안 취약점 제보** — 아래 주의사항 참고

> ⚠️ **보안 취약점은 공개 이슈로 등록하지 마십시오.**
> 암호 구현의 취약점은 비공개로 제보해야 합니다. 절차는 [`SECURITY.md`](./SECURITY.md)를 참고해 주세요.

## 개발 환경 및 빌드

- `hostap-pqc`는 단독으로 빌드하지 않습니다.
- [openwrt-pqc](https://github.com/80211-pqc/openwrt-pqc) 빌드 과정에서 패키지로 clone되어
  빌드·설치됩니다. 자세한 내용은 [README](./README.md)를 참고해 주세요.
- 변경 사항은 `openwrt-pqc` 빌드가 통과하는지, 그리고 가능하면 실제 AP/STA 환경에서
  동작하는지 확인한 뒤 제출해 주세요.

## 기여 절차

이 프로젝트는 **Fork & Pull Request** 방식으로 기여를 받습니다.

1. 이 레포를 **Fork**합니다.
2. 작업용 브랜치를 만듭니다. (예: `feat/kyber-rekey`, `fix/ptk-rollback`)
3. 변경 사항을 작업하고, **`Signed-off-by`를 포함하여** 커밋합니다. ([커밋 규칙](#커밋-규칙) 참고)
4. Fork한 레포에 push한 뒤, 이 레포로 **Pull Request**를 생성합니다.
5. 리뷰 의견에 따라 수정하고, 승인되면 병합됩니다.

> ℹ️ 큰 변경이나 설계가 필요한 작업은 PR 전에 Issue로 먼저 논의해 주세요.
> 방향을 맞춘 뒤 작업하면 재작업을 줄일 수 있습니다.

## 커밋 규칙

**Signed-off-by (DCO)**

- 모든 커밋에는 `Signed-off-by` 서명이 **필수**입니다.
- 커밋 시 `-s` 옵션을 사용하면 자동으로 추가됩니다.
  ```bash
  git commit -s -m "커밋 메시지"
  ```
- 이 서명은 [Developer Certificate of Origin](https://developercertificate.org/)에 동의함을
  의미합니다. 즉, 제출하는 코드를 해당 라이선스로 기여할 권리가 있음을 확인하는 것입니다.

**커밋 메시지**

- 제목은 간결하게, 본문에는 **무엇을 왜** 바꿨는지 적어주세요.
- PQC/802.11 관련 변경은 어떤 부분에 영향을 주는지 명시하면 리뷰에 도움이 됩니다.

**SPDX 헤더**

- 새로 작성한 소스 파일에는 SPDX 헤더를 포함합니다.
  ```c
  // SPDX-License-Identifier: BSD-3-Clause
  ```
- 기존 파일을 수정할 때는 **원본 저작권 라인을 삭제하지 마십시오.**

## 코드 스타일

- 기존 `hostap`의 코딩 스타일을 따릅니다. 주변 코드와 **일관성**을 유지해 주세요.
- 원본 프로젝트의 코딩 스타일·기여 규칙은 레포 내 원본 문서
  (`CONTRIBUTIONS`, `README.hostap`)를 참고할 수 있습니다.
- 불필요한 포매팅 변경(공백·줄바꿈만 바꾸는 변경)은 리뷰를 어렵게 하므로 지양해 주세요.

## Pull Request 체크리스트

PR을 생성하기 전에 아래를 확인해 주세요.

- [ ] `openwrt-pqc` 빌드가 통과
- [ ] 기존 기능에 회귀가 없다
- [ ] 모든 커밋에 `Signed-off-by`가 포함
- [ ] 새로 추가한 파일에 SPDX 헤더 존재
- [ ] 수정한 파일의 원본 저작권 라인이 유지
- [ ] 필요한 경우 관련 문서 업데이트

## 리뷰 및 병합

- 제출된 PR은 프로젝트 관리자의 리뷰를 거쳐 병합됩니다.
- 리뷰에서 수정 요청이 있을 수 있으며, 논의 후 반영합니다.

> ℹ️ 현재 별도의 관리자 조직이 구성되기 전까지는 프로젝트 오너가 리뷰를 담당합니다.

## 라이선스 및 DCO

- 이 레포에 기여한 코드는 프로젝트와 동일하게 **BSD-3-Clause**로 배포됩니다.
- 커밋에 `Signed-off-by`를 포함함으로써, 기여자는
  [DCO](https://developercertificate.org/)에 동의하는 것으로 간주됩니다.
