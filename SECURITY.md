# 보안 정책 (Security Policy)

`hostap-pqc`는 무선 네트워크 환경의 양자내성암호(PQC)와 프로토콜 구현을 다루는 프로젝트입니다.
보안 취약점 특성상 **협력적 공개(coordinated disclosure)** 원칙에 따라 패치가 준비될 때까지 비공개로 처리되어야 합니다. 아래 절차를 반드시 따라 주세요.

> ⚠️ **보안 취약점을 공개 이슈(Issue)나 공개 Pull Request로 등록하지 마십시오.**
> 취약점이 공개되면 수정 전에 악용될 수 있습니다. 반드시 아래 비공개 채널로 제보해 주세요.


## 목차

- [지원 범위](#지원-범위)
- [취약점 제보](#취약점-제보)
- [제보 시 포함할 내용](#제보-시-포함할-내용)
- [대응 절차](#대응-절차)
- [암호 구현 관련 주의](#암호-구현-관련-주의)
- [공개 정책](#공개-정책)
- [CVE 및 공개 조율](#cve-및-공개-조율)
- [참고 자료](#참고-자료)

## 지원 범위

- 보안 수정은 이 레포의 **기본 브랜치(최신 상태)**를 대상으로 제공됩니다.
- `hostap-pqc`는 특정 Base 스냅샷(`ca266cc2…`)에 고정되어 있습니다.
  원본 `hostap`이나 `liboqs`의 upstream 취약점은 아래
  [암호 구현 관련 주의](#암호-구현-관련-주의)를 참고해 주세요.

## 취약점 제보

취약점은 다음 **비공개 채널** 중 하나로 제보해 주세요.

- **GitHub Security Advisory:** 이 레포의 `Security` 탭 →
  `Report a vulnerability` (권장 — 비공개로 관리자에게 전달되며 CVE 발급까지 연계됩니다)
- **이메일:** `rlagh1073@naver.com`

> ℹ️ 현재 별도의 보안 대응 조직이 구성되기 전까지는 프로젝트 오너가 제보를 접수하고 대응합니다.

## 제보 시 포함할 내용

빠른 확인과 재현을 위해 다음 정보를 최대한 포함해 주세요.

- 취약점의 **유형과 영향** (예: 키 노출, 인증 우회, DoS 등)
- **재현 방법** — 단계별 절차, 필요한 설정, PoC 코드가 있으면 첨부
- **영향 받는 버전/커밋** — 확인한 커밋 해시 또는 브랜치
- **환경** — AP/STA 여부, 하드웨어·타겟, 빌드 옵션 등
- 알고 있다면 **가능한 완화책이나 수정 방향**

## 대응 절차

1. **접수 확인** — 제보를 받으면 가능한 한 빠르게(수일 내 목표) 접수 사실을 회신합니다.
2. **검증 및 분석** — 재현·영향 범위를 확인합니다. 추가 정보가 필요하면 연락드립니다.
3. **수정** — 비공개로 패치를 준비하고 검증합니다.
4. **공개 및 배포** — 제보자와 협의하여 공개 시점을 정한 뒤, 수정과 함께 공개합니다.

> ℹ️ 대응 일정은 취약점의 심각도와 복잡도에 따라 달라질 수 있습니다.
> 진행 상황은 제보자에게 공유하는 것을 원칙으로 합니다.

## 암호 구현 관련 주의

이 프로젝트는 암호 구현을 포함하므로, 다음과 같은 취약점도 제보 대상입니다.

- **사이드채널** — 타이밍, 전력 등 비공개 채널을 통한 키 정보 유출
- **구현 오류** — 상수 시간 위반, 잘못된 파라미터, 난수/키 생성 결함
- **프로토콜 결함** — 키 교환·핸드셰이크 로직의 논리적 취약점

**취약점의 위치에 따라 제보처가 달라질 수 있습니다.**

- 이 레포의 **PQC 구현·연동·최적화** 부분 → 여기로 제보해 주세요.
- 원본 **`hostap` 본체**의 취약점 → 원본 프로젝트 [w1.fi](https://w1.fi/hostap.git)에도 제보를 권장합니다.
- **`liboqs` 코어**의 취약점 → 원본 [open-quantum-safe/liboqs](https://github.com/open-quantum-safe/liboqs)에도 제보를 권장합니다.

> ℹ️ 위치가 불분명하면 우선 이 레포로 제보해 주세요. 분석 후 필요하면 upstream과 조율하겠습니다.

## 공개 정책

- 본 프로젝트는 **협력적 공개(coordinated disclosure)**를 원칙으로 합니다.
- 수정이 준비되기 전에는 취약점 세부 정보를 공개하지 않습니다.
- 원하는 제보자는 공개 시 **기여자로 표기**해 드립니다. (익명 요청도 존중합니다.)

## CVE 및 공개 조율

- 심각도가 높은 취약점은 **GitHub Security Advisory**를 통해 **CVE 발급**을 요청할 수 있습니다.
  (GitHub이 CNA 역할을 하여 CVE ID가 자동으로 연계됩니다.)
- 패치가 준비되기 전까지 세부 정보를 비공개로 유지합니다(**embargo**).
- 공개 시점은 제보자 및 관련 배포처(예: `openwrt-pqc`)와 조율합니다.
- 이는 패치 공개 이후, 아직 업데이트하지 않은 시스템이 공개된 정보로 악용되는
  위험(이른바 **1-day**)을 줄이기 위함입니다.

## 참고 자료

본 정책은 **CVD(Coordinated Vulnerability Disclosure)** 관행을 따릅니다.
배경과 절차에 대한 자세한 내용은 아래를 참고해 주세요.

- [OpenSSF OSS Vulnerability Guide](https://oss-vulnerability-guide.openssf.org/) — CVD 전체 절차와 템플릿
- [ossf/oss-vulnerability-guide](https://github.com/ossf/oss-vulnerability-guide) — SECURITY.md·공개 공지 템플릿 원본
- [GitHub: CVD for open source projects](https://github.blog/security/vulnerability-research/coordinated-vulnerability-disclosure-cvd-open-source-projects/) — 메인테이너 관점의 CVD·CVE 안내
- [CVE Program](https://www.cve.org/) — CVE 발급·CNA 제도 공식 설명